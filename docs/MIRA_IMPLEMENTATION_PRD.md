# Mira 第一阶段开发实施文档（模块化 PRD）

> 初版日期：2026-09-23；最近更新：2026-09-24
> 状态：阶段 1A 第三批已完成，实施中；不表示 P0–P3 已全部完成。
> 依据：[MIRA_PLATFORM_PLAN.md](./MIRA_PLATFORM_PLAN.md)，以该文档的产品边界与待定决策为准。  
> 适用范围：当前 `Mira/Mira-Harness` 的 Electron 桌面产品；第一阶段本仓库开发 Mira-Harness，并接入后续独立维护的 Mira Novel Studio。
> 用途：回答“改哪几个大模块、各模块按什么顺序做到可交付”；具体接口和页面交互在对应批次落地前再细化。

## 0. 更新记录

| 日期 | 更新内容 | 状态影响 |
| --- | --- | --- |
| 2026-09-24 | 同步 `Mira/Mira-Harness` 主仓库及同级 `Mira-Novel-Studio`、`Mira-Vision` 空目录的实际边界；确认阶段 0 基础架构切片已完成。 | 本 PRD 从“待评审”进入“实施中”；第一阶段整体仍未完成。 |
| 2026-09-24 | 固化阶段性完成功能和临时记录的文档规则，要求记录完成项、剩余项、验证证据和下一次开工入口。 | 后续阶段记录不得只留在聊天中。 |
| 2026-09-24 | 完成阶段 1A 第一批：移除壳层多页签，收敛设置侧栏和旧设置路由，外观页仅保留浅色/深色，并对历史 `system` 主题和旧主题色做兼容归一化。 | C 模块 P1 的设置入口收敛完成；Harness 核心任务交互和 Electron 视觉验收仍未完成。 |
| 2026-09-24 | 完成阶段 1A 第二批设计与实现：统一工作面板的运行、整理、失败、停止、完成和空闲状态呈现；不改变执行器和 IPC。 | D 模块 P1 的状态呈现切片完成；真实任务和 Electron 视觉验收仍待执行。 |
| 2026-09-24 | 补齐阶段 1A 第二批的全量验证：61 个测试文件、259 个测试通过，类型检查与 Web/Electron 构建通过。 | 自动化和产物生成验证完成；Electron 视觉、真实模型和打包版仍待验收。 |
| 2026-09-24 | 完成阶段 1A 第三批请求生命周期收口：发送/重试防重复提交，重试失败后同步持久化会话，计划确认/继续失败恢复运行状态。 | D 模块 P1 的异常恢复切片完成；真实任务闭环仍待验收。 |
| 2026-09-24 | 完成阶段 1A 开发态 Electron 首轮验收：隔离目录启动、空态、深色主题、设置入口、模型空态和项目选择器可用；无测试模型，物理鼠标与真实任务链路未通过。 | 桌面渲染基线通过；D 模块 P1 的真实任务、审批、成果和恢复仍待验收。 |

本文继续作为第一阶段的任务规划文档。每次模块边界、交付关口或验收结论发生变化时，先更新本节和对应模块，再开始下一批代码改动。

### 2026-09-24 阶段 1A 第一批记录：设置与壳层基线收敛

**已完成**

- 主布局不再渲染多页签；删除 `TabsBar` 组件和 tabs store。
- 设置侧栏收敛为基础能力、平台能力、应用三组，移除加载效果、图标库、菜单管理和自由微应用管理入口。
- 旧系统设置路径继续保留兼容重定向，统一回到 `/settings/general`；旧设置静态路由不再提供已退出页面。
- 外观页只提供浅色和深色；不再暴露系统主题、主题色、布局、圆角、组件大小、动画、水印和多页签配置。
- 主题 store 保留历史字段和方法，但启动时把旧 `system` 偏好归一化为当前实际明暗模式，把旧彩色主题回退为 Mira 默认黑白主题，不删除历史偏好结构。
- 增加设置与主题兼容回归测试，验证多页签删除、设置入口边界、外观模式、旧路由兼容以及旧主题偏好的实际归一化结果。

**验证与未完成项**

- 已通过：`npm test`（60 个测试文件、252 个测试）；`npx vue-tsc --noEmit`；`npm run build`；`npx electron-vite build`；`git diff --check`。
- 待验收：开发态 Electron 视觉与真实鼠标交互、打包安装版、真实模型和真实旧数据。
- 本批没有接入 Novel Studio、Vision、远程应用下载器、React 全量迁移或数据库迁移。

**下一次开工入口**

- 从本模块 D「Mira-Harness 默认应用」P0/P1 开始，先阅读 `docs/MIRA_PHASE0_AUDIT.md` 第 5、6 节和 Harness 现有页面/事件测试。
- 首个交互切片聚焦空态、执行态、等待确认态、失败恢复和成果入口；先形成可评审设计与状态契约，再改业务页面。

### 2026-09-24 阶段 1A 第二批记录：工作面板状态一致性

**已完成**

- 新增 [`MIRA_HARNESS_UI_SLICE_2026-09-24.md`](./MIRA_HARNESS_UI_SLICE_2026-09-24.md)，固定工作面板六种状态及交互边界。
- 工作面板现在区分 `running`、`rendering`、`failed`、`stopped`、`completed` 和 `idle`，不再把停止、失败或整理回复显示为完成。
- 当前运行状态优先使用运行 store，最近完成消息补充失败/停止/完成结果；失败错误摘要在面板中可见，重试仍由消息区负责。
- 保留工具活动、文件变更和执行详情的现有折叠/展开交互；没有新增执行器、IPC、权限或数据协议。
- 增加状态呈现纯函数测试，覆盖六种状态文案、图标、文件成果摘要和失败提示。

**验证与未完成项**

- 已通过：针对性 Vitest 15 项；`npx vue-tsc --noEmit`；`git diff --check`。
- 已通过：全量 `npm test`（61 个测试文件、259 个测试）；`npx vue-tsc --noEmit`；`npm run build`；`npx electron-vite build`；`git diff --check`。
- 待验收：开发态 Electron 视觉与真实鼠标交互、真实模型任务、打包版和跨应用切换。

**下一次开工入口**

- 从 `src/pages/frontend/harness/index.vue` 的消息区、审批卡、计划确认和输入区继续做一次真实任务状态联调。
- 重点验证“发送 → 工具执行 → 权限确认/计划确认 → 文件成果 → 失败或停止后恢复”的桌面闭环；在真实任务通过前不启动 React 正式迁移。

### 2026-09-24 阶段 1A 第三批记录：请求防重入与失败恢复

**已完成**

- 发送、重新生成和编辑重跑在异步请求期间共用页面提交锁；输入区、消息操作和错误卡同步禁用重复提交，首次建会话阶段也受保护。
- 重新生成请求失败时重新读取持久化会话，避免页面停留在乐观移除的旧状态；若主进程已截断原回复，本批不保证其恢复。发送失败仍保留草稿与附件。
- 计划确认和继续计划的 IPC 失败时清除运行标记，保留错误提示与后续可操作状态；计划确认/问答/取消增加重复提交保护。
- 新增请求生命周期回归测试，覆盖发送、重试、计划确认的连续调用及计划确认/继续失败。

**验证与未完成项**

- 已通过：`npm test`（62 个测试文件、265 个测试）；`npx vue-tsc --noEmit`；`npm run build`；`npx electron-vite build`；`git diff --check`。
- 待验收：开发态 Electron 真实鼠标与视觉、真实模型工具调用、权限确认、文件成果预览、停止后继续、打包版和跨应用切换。本批自动化只证明请求生命周期，不代表整条任务链已跑通。

**下一次开工入口**

- 从 `src/pages/frontend/harness/index.vue`、`useHarnessPageFacade.ts`、审批/计划组件和 `electron/services/harnessRuntime.ts` 出发，在隔离数据目录执行一条真实任务；逐项记录发送、工具、权限/计划、成果、失败/停止恢复的实际结果。
- 如果没有可用的测试模型配置，先完成不依赖密钥的 Electron 导航与鼠标验收，并把真实模型链路标为待验收；不得借用真实 `~/.mira`。

### 2026-09-24 阶段 1A 开发态 Electron 首轮验收记录

**已完成**

- 使用 `MIRA_TEST_HOME=/tmp/mira-electron-acceptance.I17u8Y` 启动开发态 Electron，未读取真实 `~/.mira`；渲染地址为 `http://127.0.0.1:9002/`。
- 窗口加载 Harness 空态，标题为“新对话 - Mira Harness”；侧栏、三张快捷任务卡、项目选择、权限档位、执行方式、模型选择和发送按钮均可见。
- 使用 Electron DevTools 截图检查浅色/深色主题；设置面板打开后只显示外观、选择应用、帮助与更新等当前收敛入口。
- 模型选择器在无模型配置时显示“没有可用模型，请先完成模型配置”；项目选择器在无项目时显示“没有匹配的项目 / 新建项目”。
- 通过 DevTools `Input.dispatchMouseEvent` 触发项目选择器和快捷任务卡，项目弹层能打开，快捷卡能把文本填入输入框；无模型时发送按钮仍保持禁用。

**验证与未完成项**

- 已通过：开发态 Electron 隔离目录启动与渲染；首屏和深色主题截图；设置、模型、项目和无模型发送边界的 DOM/事件检查。
- 仅为模拟输入：DevTools `Input.dispatchMouseEvent` 不是操作系统物理鼠标；本机未执行物理鼠标验收。
- 待验收：真实模型任务、工具执行、权限确认、计划确认、文件成果预览、失败/停止后继续、打包版和跨应用切换。

**下一次开工入口**

- 有可用测试模型后，继续从 `src/pages/frontend/harness/index.vue` 与 `electron/services/harnessRuntime.ts` 验证“发送 → 工具 → 权限/计划 → 文件成果 → 完成/失败/停止恢复”。
- 若当前环境仍无测试模型，先保持代码不变，等待模型配置或安排可执行的物理鼠标验收；不要用真实用户目录代替隔离验收。

### 阶段记录要求

每次阶段性功能开发完成，或用户要求“临时记录”时，必须同步记录：

1. 日期和记录标题。
2. 本次已完成的功能、文档或决策。
3. 剩余问题、未完成事项和明确的风险。
4. 已执行的测试、构建和桌面验收；未执行的项目标记为“待验收”。
5. 下一次从哪个文档、章节、模块或文件入口开始。

PRD 的更新记录保存任务规划和阶段结论；交接文档保存可直接续作的当前快照；平台方案只记录产品边界和架构决策；阶段审计只记录源码证据、风险和技术选型。四份文档发生关联变化时一起更新，不用聊天记录替代项目记录。

## 1. 目标、非目标与口径

**交付目标**：一个可安装的 Mira 桌面客户端，离线首次启动可进入内置 Harness；Novel Studio 作为受管理的第一方子应用安装、切换和更新；两者共享必要的平台能力，但保留各自界面和数据。任务、审批、草稿、成果在切换或重启后按约定恢复。

**不在第一阶段**：Mira Vision 业务、任意第三方插件市场、可执行代码安装包、云同步、完整 Git IDE、媒体生成适配、差分更新、复杂多 Agent 编排及移动端。Vision 的同级空目录已预留，但尚无 Git 仓库或业务工程；本仓库只规划未启用的接入配置，不进入应用列表、设置、构建或发布。

本文件是**开发实施 PRD**，不是当前实现状态清单，也不是立即删除旧小说代码的命令。Novel Studio 与 Vision 的业务工程以后各自建 Git 仓库维护；本仓库最终仅保留其必要的平台接入配置。当前小说页面和存储仍在本仓库，须在独立包、旧作品读取及回退验收后迁出，用户数据不得随源码删除。模型供应商的数据结构、React 选型及应用包协议细节在实施时单独评审，不在这里虚构成既定接口。

### P0–P3 表示什么

| 关口 | 含义 | 放行条件 |
| --- | --- | --- |
| P0：界定与基线 | 确定该模块保留、退出和迁移的边界；准备设计、现状证据与可复现的核心场景 | 明确负责人、依赖、验收用例和回退入口；待定选项有明确决定时点 |
| P1：最小可用改动 | 只改能形成一条真实闭环的代码，保留现有用户数据 | 本模块的核心场景可在开发版使用，针对改动有自动化检查 |
| P2：双应用集成 | 接入真实平台、实际数据和应用切换；处理旧版本兼容 | Harness 与 Novel Studio 的相关链路联调通过，异常路径可恢复 |
| P3：发布验收 | 生产包、旧数据、失败恢复及桌面交互验收 | 满足模块验收；macOS、Windows 和真实模型的证据分别记录，不以构建代替运行验收 |

P 是**同一模块内部的完成关口**，不是需求优先级，也不要求所有模块的 P1 同时开工。每个模块必须逐关口通过，且满足跨模块依赖，才可宣称第一阶段完成。若评审决定保留 Vue 而不使用 React，仍通过 Harness 模块的 P0–P3，不强制进行 React 迁移。

## 2. 改动总览与依赖

第一阶段分为 **7 个大模块**，另有贯穿各模块的数据与验收要求：

| 模块 | 主要归属 | 核心产物 | 先决条件 |
| --- | --- | --- | --- |
| A. Electron 平台服务 | 桌面主程序 | 清晰的服务、IPC、模型与安全边界 | 现有服务和 IPC 清单 |
| B. Platform SDK 与长任务 | 宿主 + 两应用适配层 | 版本化能力、应用身份、任务查询与成果引用 | A 的能力所有者确定 |
| C. Vue Shell 与统一设置 | 桌面壳 | 固定导航、应用切换、新设置页、昼夜主题 | 应用清单与设置归属明确 |
| D. Mira-Harness | 默认应用 | 真任务工作台、审批、历史与成果 | B 的事件和任务契约 |
| E. Mira Novel Studio 接入 | 独立小说项目 + 本仓库接入配置 | 外部静态包、旧作品与生成闭环；主仓库退出小说业务源码 | B 的模型、存储与生命周期契约 |
| F. 应用中心与分发 | 主程序 + Shell | 官方应用目录、安装更新与恢复 | B、C、E 的入口和版本约定 |
| G. 数据迁移与质量 | 主程序 + 两应用 | 旧数据保全、回归矩阵与发布证据 | 随 A–F 并行，发布前收口 |

建议的**最短验证路径**：先审计现有 Electron/Harness/小说依赖并调研开源 Agent UI，明确 A/B/C 的边界和 D 的选型；用现有 Harness 和小说业务验证 SDK，再由独立 Novel Studio 项目提供本地静态包并完成 E 的接入与旧作品验收；最后 F 接远程 Release 和安装事务，由 G 贯穿并完成总验收。旧结构下的原型测试只是技术摸底，不能充当新架构验收；安装器不能替代外部子应用闭环。

阶段 0 的实际源码审计与候选初筛见 [MIRA_PHASE0_AUDIT.md](./MIRA_PHASE0_AUDIT.md)。它记录第一批改动建议，不把本 PRD 的 P0–P3 自动标记为通过。

## 3. 模块关口

### A. Electron 平台服务

**改动边界**：拆解 `electron/main.ts`、`electron/preload.ts` 中集中职责，按启动装配、IPC、业务服务、存储、外部适配、安全边界逐块迁移；不是一次性重排目录。Harness 运行时、模型凭据与文件访问由桌面侧负责；现有小说存储在迁出过渡期保持可用，后续是否继续作为平台服务由数据归属评审决定，不让独立项目直接依赖主进程内部源码。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 列出窗口生命周期、IPC 调用方、模型/供应商、Harness 任务、小说 API 与数据路径；标记哪些是宿主私有接口。选定首批切分目标与无行为变更的回归用例。 |
| P1 | 入口只装配首批已切分的服务；IPC 做应用身份、参数和权限检查，业务行为仍由服务执行。现有 Harness 会话与小说读写继续可用；相关单测及类型检查通过。 |
| P2 | SDK 所需的模型、任务、成果、存储服务由平台适配提供；密钥留在主进程，前端不可直接得到全量 preload。切换和卸载页面不终止宿主任务。 |
| P3 | 打包后在 macOS / Windows 分别验证窗口、原生菜单、文件访问、长任务和模型调用；错误可定位，旧 IPC 调用方未误断。 |

### B. Platform SDK 与长任务

**改动边界**：从现有 IPC 提炼 Context、Models、Harness、Tasks、Artifacts、Storage、Navigation 的最小接口；独立子应用按身份调用允许的能力，不直接依赖本仓库的 Pinia、Electron 内部模块或其他应用的数据表。Wujie 只承载界面与生命周期，不承担安装器或任务执行器。本仓库应用配置只声明受控身份、入口、版本、能力与安装来源，不替代外部项目的业务实现。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 定义首版接口字段、调用方身份、必要能力、错误码、订阅清理和 Shell/API/应用三类版本兼容规则；模型连接、能力描述、应用默认绑定分离，但数据结构需评审。 |
| P1 | 一条 Harness 真任务经适配层提交并取得 `taskId`；页面关闭后可查询状态。任务记录所属应用、业务对象和成果引用；创建支持幂等标识，取消区分请求与终态。 |
| P2 | 小说经 SDK 调用真实文本模型与本应用存储；事件有任务 ID 和递增序号，重新挂载时快照加游标恢复或去重。审批在跨应用切换后仍可找到，能力不可用返回明确错误。 |
| P3 | 旧应用/新宿主、缺少能力、订阅重放、重启后状态、取消与重复点击均有测试；破坏性 API 升主版本，不兼容安装在激活前被阻止。 |

### C. Vue Shell 与统一设置

**改动边界**：Shell 只负责窗口公共区域、应用选择、任务入口、昼夜主题和统一设置；Harness 会话导航与小说编辑器工具由各应用负责。新设置页按基础、平台能力、模型与应用绑定、应用中心、应用专属、关于分组；项目/作品参数留在应用工作区。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 出简洁设计稿，标出固定导航、应用切换、设置返回、空态/错误态及哪些原功能退出；确认设置与应用工作区边界，再动业务页面。 |
| P1 | 默认进入内置 Harness；白天/黑夜可切换。清理自由微应用录入、菜单树管理、壳级路由多页签及布局/主题色/加载动效/图标库的用户配置，包括专用路由、状态和无用分支；保留系统菜单、业务切换及必要加载反馈。 |
| P2 | 同一设置入口管理实际可用的供应商连接、模型能力和两应用默认模型；切换到已安装小说，主题和导航一致，任务入口能回到来源应用，不出现双侧栏/双顶栏。开发入口仅在本机配置可见。 |
| P3 | 旧设置路由回到有效入口，旧偏好不复活退出功能；历史会话、作品、模型配置未误删；两端桌面验证标题栏拖动、焦点、弹层、输入法及真实鼠标点击。 |

### D. Mira-Harness 默认应用

**改动边界**：以现有执行服务和会话数据为基线，先打磨核心任务流程。React 与开源 Harness 只是候选；设计收益与框架收益分开评估，禁止未验证即替换生产页。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 用相同设计覆盖空态、执行、确认、成果、失败恢复；确定“发送 → 工具 → 确认 → 文件成果 → 再进入会话”原型。对照 Vue 改造、React UI、完整开源项目的依赖/许可证/适配成本，记录选择。 |
| P1 | 选定路线接入真实事件，输入区明确模型和权限；执行中、等待确认、失败、停止、完成状态一致；工具细节可展开，不重复堆砌过程。 |
| P2 | 运行时切到小说再回来，审批、停止、计划、会话历史、成果预览正常且没有重复订阅；运行期间的下一条草稿可保存，不将“排队/中途指令”擅自纳入本批。 |
| P3 | 长会话、中文输入法、剪贴板、文件选择、焦点和滚动在桌面包验收；真实模型任务完成后可打开成果。文件 diff 可另排期，安全撤销需独立快照与冲突设计，不因展示 diff 即默认支持。 |

### E. 独立 Mira Novel Studio 的接入

**改动边界**：从已有 Vue 小说页面和主进程存储出发，把小说界面、应用端业务逻辑与构建发布迁往独立项目；本仓库仅保留接入配置、通用平台接口及经评审仍由平台提供的存储服务。小说保有作品、章节、草稿和生成流程；不把存储换成浏览器 `localStorage`，也不因拆仓库直接迁移数据库。Vision 仅有禁用的预留配置，不创建业务工程。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 清点作品/章节、生成、模型绑定和文件路径；明确数据唯一所有者、应用 ID 映射、资源基路径、开发入口、挂载/卸载/恢复协议及独立开发时的 mock 边界。确定旧业务源码的迁移清单、独立 Git 项目的建立时点及保留旧入口的回退方案。 |
| P1 | 独立项目中的小说前端可构建为静态包，经本仓库受控配置在同一 Electron 中加载；通过 SDK 读取现有作品，订阅在卸载时释放。开发 URL 仅本地联调，先验证 Wujie 对 URL、HMR、资源路径与路由的真实支持。 |
| P2 | 真实模型可完成一次小说生成，作品与正文保存到既有持久化；切换、重新挂载和应用重启后草稿/正文正确恢复，任务结果可定位。浏览器独立开发的 mock 不冒充生产能力。 |
| P3 | 新旧作品、长文本编辑、模型失败重试、切换中保存及主题同步通过桌面验收；无密钥暴露、跨应用越权或重复生成。确定无需旧入口回退后再移除本仓库小说业务源码；数据按独立迁移方案保留。Vision 不混入小说构建与发布。 |

### F. 应用中心与分发

**改动边界**：生产环境仅显示平台批准的第一方应用。第一期默认 Harness 随桌面安装包内置，Novel Studio 为预构建静态 ZIP；不执行下载包里的脚本，不把 GitHub 的 `latest` 直接当信任清单。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 评审第一期包类型、独立仓库归属、包内 manifest、本仓库第一方应用配置、外部发布元数据、版本与能力检查、可信发布来源；确定 `MiraPaths` 下程序资源/用户数据分离及激活版本记录方式。Vision 配置保持禁用，不生成下载条目。 |
| P1 | 使用本地 Novel Studio 包完成清单/入口/能力校验、版本目录安装、激活和受控加载；已安装版离线可打开。摘要校验不代替可信来源/签名验证。 |
| P2 | 应用中心按批准目录下载指定 Release，临时区下载、校验来源与摘要、限制解压内容/路径/大小，完整安装后原子激活；权限扩大明确提示，安全切换前保存草稿。 |
| P3 | 网络中断、损坏包、解压失败、缺能力、入口加载失败均保留旧版和数据；兼容时可恢复旧资源版，不承诺不兼容数据格式回滚。卸载默认保留作品与历史，清理用户数据须独立确认。 |

### G. 数据迁移与质量

**改动边界**：这是跨模块的发布关口，不为了目录整齐重写 SQLite。旧用户模型配置、Harness 会话、小说作品与成果链接尽量复用现有数据；静态检查、真实运行和平台验收分别记录。

| 关口 | 交付与验收 |
| --- | --- |
| P0 | 建立旧版本样本和回归矩阵：首次启动/无模型、历史会话与作品、旧设置偏好、原微应用 ID、应用切换、失败恢复；记录数据根目录及备份/回退方案。 |
| P1 | 对确需变更的应用 ID、设置或存储做显式版本映射；停止旧偏好生效不等于删除用户记录。必要迁移必须事务化、保留可恢复旧数据并支持失败重试，不做无约束双写。 |
| P2 | 用真实旧数据验证记录数、引用、正文、模型绑定与成果链接；故意中断迁移/安装后能重试或使用原有入口，应用更新与业务数据版本的兼容范围明确。 |
| P3 | 分别记录类型检查、单测、构建、打包结果，以及 macOS Electron、Windows Electron、真实模型调用和真实鼠标操作的验收结果；未做的项标为待验收，不写成“已通过”。 |

## 4. 批次放行与第一阶段完成标准

1. **首批评审**：先完成现有依赖审计和开源 Agent UI 候选对比，确定 A/B/C 的职责、UI 路线、首个真实任务用例、小说迁出清单和数据回退方案。此时不同时启动 React 全量迁移、数据库重写、仓库拆分与安装器上线。
2. **平台与本地双应用闭环**：A、B、C、D、E 至少通过各自 P2，G 完成相应旧数据检查；本仓库 Harness 与独立 Novel Studio 包可在同一 Electron 中完成真实任务，切换不丢任务和草稿。
3. **分发闭环**：F 通过 P0–P3，G 完成安装失败与数据兼容检查；安装更新不破坏旧版本或用户数据。
4. **最终发布**：A–G 均通过 P3；首次离线启动、真实模型、两应用切换、历史恢复、下载损坏/不兼容/回退、主题和生产入口按原方案第 13.2 节逐项验收。

每一批实施前把本文件对应关口拆成可执行任务：**改动入口、接口差异、数据影响、自动化用例、桌面验收设备、回退操作和负责人**。若评审改变仓库、平台归属、包类型或数据所有权，先更新原方案和本文件，再开始该批改动。本文所有 P0–P3 当前均为规划，不能当作完成标记。
