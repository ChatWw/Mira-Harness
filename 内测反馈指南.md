# Mira 内测反馈指南

感谢你参与 Mira Harness 的内测 🙏

这是一个**还在开发中的版本**，很多地方不完善，你的每一条反馈都会直接影响它接下来怎么改。

---

## 一、先说清楚：内测版是什么状态

**能用起来的：**

- **Agent 工作台** —— 新建对话、选择项目文件夹、让 Mira 读写项目文件、查看运行步骤与工具调用
- **权限控制** —— 默认 / 自动批准 / 完全，三种模式可切换
- **模型管理** —— 支持 GLM、Kimi、MiniMax、DeepSeek、Ollama 预设，也可填自定义 OpenAI 兼容端点
- **计划模式** —— 复杂任务先出计划、你确认后再执行
- **子任务委派** —— 复杂任务拆成子任务并行跑，可以单独看每个子任务的过程
- **记忆** —— Mira 会记住你告诉它的事，涉及敏感信息时会先问你
- **自动化** —— 定时任务、单次任务、会话完成触发，有运行记录
- **AI 小说创作** —— 作品设定 → 故事总纲 → 章节细纲 → 正文的完整链路
- **MCP / Git / Python 环境** 等设置项

**还没做完的（不用提，已知）：**

- Slash 命令（输入 `/`）目前是固定菜单，还不能扩展
- 自动化只支持"定时"和"会话完成触发"，**文件变化触发、Webhook 还没做**
- 自动化任务错过时间点不会自动补跑
- 后台通知还没有
- 系统级记忆还是预留状态

**界面上的小瑕疵**（对齐、间距、个别地方暗色模式不好看）我自己也发现了一些，正在处理——如果你发现了也欢迎提，但这类问题的优先级会排在功能问题后面。

---

## 二、怎么提反馈

**统一提到 GitHub Issues：**

👉 https://github.com/ChatWw/Mira-Harness/issues/new/choose

点进去会让你选类型：

| 类型 | 什么时候用 |
| --- | --- |
| 🐛 **问题反馈** | 报错、闪退、卡住、显示不对、行为和预期不符 |
| 🚫 **安装或启动失败** | 装不上、打不开、白屏、被系统拦截 |
| 💡 **功能建议** | 希望增加什么，或者哪里可以更好用 |

**需要 GitHub 账号。** 如果没有，或者实在不方便用 GitHub，也可以发邮件到 **tuyn53@163.com**，但走 Issues 处理会更快、也不容易漏掉。

> 提之前麻烦先搜一下 Issues 列表，看看有没有人已经提过同样的问题——重复的我会合并，但你的补充信息依然有价值。

---

## 三、提问题前，先准备这三样

这三样信息决定了问题能不能被快速定位。**能提供的话尽量提供，提供不了就先提，我们再聊。**

### 1️⃣ 版本号

打开 **设置 → 关于**，把「版本信息」那一行抄下来（例如 `v0.1.0`）。

内测期间我会频繁更新，没有版本号很难判断你说的是哪个版本的问题。

### 2️⃣ 日志

Mira 的日志在**用户目录下的 `.mira/logs`** 文件夹里。

**macOS 怎么进：**

1. 打开「访达」（Finder）
2. 按 `Command + Shift + G`
3. 粘贴这个路径，回车：
   ```
   ~/.mira/logs
   ```
4. 里面会有几个 `.jsonl` 文件，找**最近修改的那个**，用文本编辑器打开，把**最后 100 行左右**复制出来

**Windows 怎么进：**

1. 打开「文件资源管理器」
2. 在**地址栏**里粘贴这个路径，回车：
   ```
   %USERPROFILE%\.mira\logs
   ```
3. 里面会有几个 `.jsonl` 文件，找**最近修改的那个**，用记事本打开，把**最后 100 行左右**复制出来

> ⚠️ **`.mira` 是隐藏文件夹**，直接翻是翻不到的，必须用上面的路径跳进去。
>
> ⚠️ **粘贴日志前扫一眼内容**，日志做过脱敏，但保险起见不要把你的 API Key 贴到公开的 Issue 里。

**如果应用完全打不开**，这个文件夹可能不存在，那就跳过日志，重点给截图。

### 3️⃣ 截图或录屏

界面上任何不对劲的地方，直接截图。**一张截图胜过一段文字描述。**

提交 Issue 的时候，把图片直接拖到输入框里就能上传。

---

## 四、关于你的数据

Mira 是**本地优先**的应用：

- 你的项目数据、对话记录、模型密钥都存在**你自己电脑上**（`~/.mira` 目录）
- **不会上传到任何服务器**
- 你提到 Issue 里的日志和截图是**你自己决定要不要公开**的

所以：

- 贴日志和截图前，**先看一眼有没有不想公开的内容**（比如项目里的文件路径、文件名、代码片段）
- API Key 千万别贴出来

---

## 五、反馈之后会怎样

内测阶段我会尽量快地处理：

- 能复现的问题 → 修好，标注版本号
- 复现不了的 → 我会在 Issue 里追问你几个问题，麻烦配合
- 功能建议 → 我判断，合适的会排进计划
- 已知问题（上面第二节列的）→ 会标记为「已知」，不用重复提

**不要客气，也不要担心问题太蠢。** 内测的意义就是让你替我发现我测不出来的问题——你遇到的每一个卡顿、每一个"这什么鬼"，对我来说都是有效信息。

---

*最后更新：2026-09 · 适用于 Mira 内测版*
