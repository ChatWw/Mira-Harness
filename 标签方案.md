# Mira Harness Issue 标签方案

GitHub 仓库创建标签：**Issues → Labels → New label**，或用 `gh label create`。
下面是建议的一套，够用且不复杂。

## 类型标签（每条 Issue 有且只有一个）

| 标签 | 颜色建议 | 用途 |
| --- | --- | --- |
| `bug` | `#d73a4a` 红 | 功能出错、行为不符预期 |
| `安装问题` | `#b60205` 深红 | 装不上、打不开、白屏、被系统拦截 |
| `增强` | `#a2eeef` 浅青 | 功能建议、体验改进 |
| `文档` | `#0075ca` 蓝 | 说明文档有误或缺失 |

## 状态标签（表示处理进度）

| 标签 | 颜色建议 | 用途 |
| --- | --- | --- |
| `待确认` | `#fbca04` 黄 | 新提交、还没看过 |
| `需要信息` | `#d4c5f9` 淡紫 | 缺版本/日志/复现步骤，等作者补充 |
| `已复现` | `#f9d0c4` 橙 | 我这边能稳定重现了 |
| `修复中` | `#1d76db` 蓝 | 正在修 |
| `已修复` | `#0e8a16` 绿 | 已修好，等待下个内测包验证 |
| `无法复现` | `#cccccc` 灰 | 试过但重现不了，暂时挂着 |

## 特殊标签

| 标签 | 颜色建议 | 用途 |
| --- | --- | --- |
| `已知问题` | `#e4e669` 黄 | 内测说明里已列出的待做项，**不用重复提** |
| `不修复` | `#ffffff` 白 | 有意为之 / 超出当前范围 |
| `好建议` | `#5319e7` 紫 | 值得认真考虑的功能建议 |

---

## 建议的处理流程

```
新 Issue 进来
  └─ 自动带 `待确认`（模板里已配置）
      ├─ 信息不全 → 加 `需要信息`，在评论里问
      │    └─ 作者补充后 → 去掉 `需要信息`，回到下面
      ├─ 是已知待做项 → 换 `已知问题`，说明在计划里，关闭
      ├─ 能重现 → `已复现` → `修复中` → 修好 `已修复`，等下个包验证后关闭
      └─ 重现不了 → `无法复现`，先挂着，看有没有别人也遇到
```

---

## 用 gh CLI 批量创建（可选）

如果本地装了 `gh` 并且登录了：

```bash
gh label create "安装问题" --color "b60205" --description "装不上、打不开、白屏"
gh label create "待确认" --color "fbca04" --description "新提交、还没看过"
gh label create "需要信息" --color "d4c5f9" --description "缺版本/日志/复现步骤"
gh label create "已复现" --color "f9d0c4" --description "能稳定重现"
gh label create "修复中" --color "1d76db" --description "正在修复"
gh label create "已修复" --color "0e8a16" --description "已修好，等待验证"
gh label create "无法复现" --color "cccccc" --description "重现不了"
gh label create "已知问题" --color "e4e669" --description "内测说明里已列出，不用重复提"
gh label create "不修复" --color "ffffff" --description "有意为之 / 超出范围"
gh label create "好建议" --color "5319e7" --description "值得认真考虑的建议"
```

> 注：`bug`、`enhancement`、`documentation` 这几个 GitHub 默认就有，
> 你可以直接把 `enhancement` 改名为「增强」、`documentation` 改名为「文档」，
> 也可以删掉重新建中文的（模板里的 `labels` 字段要同步改）。

---

## 小提示

- GitHub 默认标签是英文的（`bug`、`enhancement`...）。模板文件里用的是 `增强`、`安装问题` 这类中文标签，**如果标签不存在，GitHub 会自动创建**（颜色是随机的，你可以事后改）。
- 标签不宜多。上面这套 13 个已经够内测用了，标签太多你自己也懒得打。
